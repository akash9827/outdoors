var mongoose= require('mongoose');
var Schema= mongoose.Schema;

var ArenaPhotoSchema= new Schema({
	arena:{type: mongoose.Schema.Types.ObjectId, ref: 'Arena'}
})

module.exports= mongoose.model('ArenaPhoto',ArenaPhotoSchema);